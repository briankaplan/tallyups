# Force Python provider (override Node.js detection from package.json)
providers = ["python"]

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "poppler_utils"]

[phases.install]
cmds = [
    "python -m venv /opt/venv",
    "/opt/venv/bin/pip install --upgrade pip",
    "/opt/venv/bin/pip install --no-cache-dir -r requirements.txt",
    "echo 'Fresh install completed: 2025-12-03T05:05'"
]

[phases.build]
cmds = ["echo 'Build v2025.12.11 - Added generate-images endpoint'"]

[start]
cmd = "/opt/venv/bin/gunicorn viewer_server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
