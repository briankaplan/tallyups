# Flask web framework
# Cache bust: 2025-12-03 - force fresh pip install
flask>=3.0.0
flask-cors>=4.0.0
flask-compress>=1.14.0  # Gzip compression for faster API responses
flask-limiter>=3.5.0  # Rate limiting for security
flask-wtf>=1.2.0  # CSRF protection

# Security
bcrypt>=4.0.0  # Secure password hashing
pyotp>=2.9.0  # TOTP for 2FA
qrcode[pil]>=7.4.0  # QR code generation for 2FA setup
PyJWT>=2.8.0  # JWT verification for Plaid webhooks

# AI/OCR - Gemini (FREE tier!)
google-generativeai>=0.8.0
pillow>=10.0.0
pillow-heif>=0.13.0

# OpenAI
openai>=1.0.0

# Google APIs (Calendar, Gmail)
google-auth>=2.25.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.111.0

# Cloud storage (Cloudflare R2)
boto3>=1.34.0

# Database - MySQL
pymysql>=1.1.0
cryptography>=41.0.0

# Plaid Bank Integration
plaid-python>=17.0.0

# PDF/HTML processing
PyMuPDF>=1.23.0  # Pure Python PDF processing - no external dependencies!
pdf2image>=1.16.0  # Fallback - requires poppler (via nixpacks.toml)

# Browser automation for HTML email screenshots
playwright>=1.40.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
gunicorn>=21.2.0
numpy>=1.24.0  # Required by pandas
pandas>=2.0.0
openpyxl>=3.1.0  # Excel export support

# Background scheduler for automatic inbox scanning
APScheduler>=3.10.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.2.0

# Updated: Tue Dec 16 06:30:49 CST 2025
# Trigger deploy: Tue Dec 16 07:19:59 CST 2025
# Deploy: Tue Dec 16 07:29:47 CST 2025
